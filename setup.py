from setuptools import setup

setup(
    name = "webmonitor",
    version = "0.0.1",
    author = "Blue",
    description = ("Simple website monitor"),
    license = "MIT",
    keywords = "monitor web",
    packages=['monitor', 'tests'],
)